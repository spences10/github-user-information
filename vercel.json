{
  "builds": [
    {
      "src": "api/card.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [{ "src": "/card.jpg", "dest": "/api/card.ts" }],
  "env": {
    "GITHUB_GRAPHQL_TOKEN": "@github_graphql_token"
  }
}
